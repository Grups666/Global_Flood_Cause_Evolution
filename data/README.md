# Data

## `reference/`

Small external reference assets used for rendering, with stable source names.
The current asset is the Natural Earth 1:110m admin-0 country archive.

## `derived/`

Project-owned, reproducible Parquet files:

- `catchment_metadata.parquet`
- `event_features.parquet`
- `annual_maximum_events.parquet`
- `pot_q95_events.parquet`

These files are generated by the pipeline and ignored by version control.

## Read-only source

Multi-gigabyte source data remain in:

```text
D:/MyPaper/papers/Event_Typology/submission/data/Global Data
```

They must not be modified or duplicated here.

